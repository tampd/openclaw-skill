# Memory — 2026-03-16

- Kiểm tra cron sáng: `morning-briefing` bị treo do prompt dùng sai path `workspace/memory/...` thay vì path tuyệt đối hoặc path trong workspace chuẩn; job wake lên nhưng không hoàn tất nên còn `runningAtMs`.
- `weekly-memory-cleanup` cũng có dấu hiệu treo cùng kiểu: job được kích hoạt nhưng không clear state sau khi chạy.
- Đã viết và publish bài blog theo lịch: `OpenAI Agents SDK hỗ trợ MCP: Developer Python được lợi gì?` — Post ID 179 — https://blog.chaiko.info/openai-agents-sdk-mcp/
- Đã kiểm tra lại fanpage Tý Tech: kết nối Graph API đã hoạt động trở lại, nhưng các Post ID cũ ngày 14-15/03 đều trả về lỗi `(#10) Object does not exist / missing permission` khi truy vấn trực tiếp.
- Đã đăng lại 1 bài mới cho fanpage Tý Tech có ảnh: `Có nên luôn online sau giờ làm không?` — Post ID `1012410591957125_122101534083074170` — https://www.facebook.com/122101530363074170/posts/122101534083074170
- Ảnh bài mới dùng bộ riêng trên Wikimedia Commons, upload thành media IDs 176, 177, 178.
- Sếp muốn Tôm chủ động tự kiểm tra và sửa các lỗi liên quan để làm việc tốt và chính xác hơn; ưu tiên tự fix trong phạm vi an toàn, rồi báo kết quả ngắn gọn.
- Đã khôi phục lại 3 job lịch đăng bài cho fanpage Tý Tech trong `~/.openclaw/cron/jobs.json`: `tytech-morning` (07:45 GMT+7), `tytech-noon` (12:15 GMT+7), `tytech-evening` (20:00 GMT+7).
- Sếp chốt quy trình vận hành mới: trước khi thực thi phải báo ngắn đang làm gì; sau khi làm xong phải tự kiểm tra kết quả thực tế rồi mới báo cáo. Nếu phát hiện lỗi hệ thống/token/permission/scheduler/heartbeat có thể ảnh hưởng công việc thì phải báo ngay để xin ý kiến sửa, không được âm thầm bỏ qua.
- Đã phát hiện và gỡ trạng thái kẹt `runningAtMs` của job `daily-report` trong `~/.openclaw/cron/jobs.json`; sau đó scheduler/Gateway nhặt lại job, xác nhận Gateway vẫn đọc file cron.
- Đã sửa hướng dẫn heartbeat cho Tý Tech: query đọc feed/comment đúng cú pháp Graph API và có rule không được lặp lại cùng một lệnh lỗi ở heartbeat kế tiếp nếu chưa có thay đổi.
- Đã xác minh thực tế: Graph API hiện đọc được feed của fanpage Tý Tech, nhưng đọc inbox vẫn lỗi quyền `(#200) Requires permission: pages_messaging or User associated with the Page access token does not have an appropriate role on the Page.`
- Đã tạo checklist vận hành `notes/tytech-ops-checklist.md` để chuẩn hóa các bước: báo trước khi làm, verify sau khi đăng, ghi Post ID/link thực tế, và báo ngay khi gặp lỗi token/permission/scheduler.
- Sếp yêu cầu thêm nguồn `Alex AI Update` vào danh sách theo dõi 2 lần/ngày để nhặt ý tưởng hay, rồi viết lại ngắn gọn theo giọng riêng của Tý Tech; ưu tiên format ngắn, rõ, không copy nguyên.
- Bài học vận hành cron Tý Tech: không dùng job kiểu cũ `prompt + target + sessionTarget: main` cho tác vụ đăng bài cần chạy đúng giờ. Kiểu này có thể wake job nhưng không dispatch được vào main session, để lại `runningAtMs` mà không có execution thật. Với tác vụ định kỳ quan trọng phải dùng schema chuẩn `sessionTarget: isolated` + `payload.kind: agentTurn` + `delivery.mode: announce`, rồi verify lại file `jobs.json` sau khi sửa.
- Đã khắc phục 3 job `tytech-morning`, `tytech-noon`, `tytech-evening` sang schema isolated chuẩn; đồng thời xóa trạng thái kẹt `runningAtMs` của slot sáng để không giữ cờ treo cũ.
